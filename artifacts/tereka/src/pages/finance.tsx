import { useEffect, useState } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { Link } from 'wouter';
import { ArrowDownLeft, ArrowUpRight, Check, Edit3, Filter, Landmark, MoreHorizontal, Plus, RefreshCw, Search, Send, Sparkles, Target, Trash2, Wallet } from 'lucide-react';
import {
  Currency, ProfileTheme, TransactionType, useArchiveAccount, useCreateAccount, useCreateBudget, useCreateCategory, useCreateConversation, useCreateGoal, useCreateTransaction, useDeleteBudget, useDeleteGoal, useDeleteTransaction, useGetAccounts, useGetBudgets, useGetCategories, useGetConversationMessages, useGetConversations, useGetDashboardSummary, useGetGoals, useGetInsights, useGetProfile, useGetTransactions, useSendAssistantMessage, useUpdateAccount, useUpdateBudget, useUpdateGoal, useUpdateProfile, useUpdateTransaction,
  getGetAccountsQueryKey, getGetBudgetsQueryKey, getGetCategoriesQueryKey, getGetConversationMessagesQueryKey, getGetConversationsQueryKey, getGetGoalsQueryKey, getGetProfileQueryKey, getGetTransactionsQueryKey,
} from '@workspace/api-client-react';
import type { Account, Budget, Category, Conversation, Currency as CurrencyType, FinancialGoal, Transaction, TransactionType as TransactionTypeValue } from '@workspace/api-client-react';
import { AppShell, Button, Card, EmptyState, Field, Modal, PageHeading, Skeleton, inputClass } from '@/components/layout';
import { DashboardCharts } from '@/components/finance-charts';
import { compactMoney, dateLabel, money, transactionIcon } from '@/lib/finance';
import { applyTheme } from '@/lib/theme';

const currencies = Object.values(Currency);
const txTypes = Object.values(TransactionType);
function ErrorState({ retry }: { retry: () => void }) {
  return <div className="rounded-2xl border border-destructive/20 bg-destructive/5 p-6 text-center"><p className="font-serif text-xl">That view needs a moment.</p><p className="mt-2 text-sm text-muted-foreground">We couldn't load this part of your money picture.</p><Button variant="secondary" onClick={retry} className="mt-4" data-testid="button-retry"><RefreshCw size={15} /> Try again</Button></div>;
}

function StatCard({ label, value, detail, accent = 'primary', icon: Icon }: { label: string; value: string; detail: string; accent?: string; icon: typeof Wallet }) {
  return <Card className="relative overflow-hidden p-5"><div className={`absolute right-0 top-0 h-20 w-20 translate-x-7 -translate-y-7 rounded-full ${accent === 'gold' ? 'bg-accent/20' : 'bg-primary/10'}`} /><div className="relative flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 text-2xl font-extrabold tracking-tight">{value}</p><p className="mt-2 text-xs text-muted-foreground">{detail}</p></div><span className={`grid h-9 w-9 place-items-center rounded-xl ${accent === 'gold' ? 'bg-accent/20 text-primary' : 'bg-secondary text-primary'}`}><Icon size={17} /></span></div></Card>;
}

function Dashboard() {
  const summaryQuery = useGetDashboardSummary(); const insightQuery = useGetInsights();
  const summary = summaryQuery.data; const insights = insightQuery.data || [];
  if (summaryQuery.isLoading) return <AppShell><DashboardSkeleton /></AppShell>;
  if (summaryQuery.isError || !summary) return <AppShell><ErrorState retry={() => summaryQuery.refetch()} /></AppShell>;
  return <AppShell><PageHeading eyebrow="Wednesday, 12 June" title="Your money, in focus." description="A quiet overview of the choices you've made and the progress they are creating." action={<Link href="/transactions" className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground shadow-sm hover:opacity-90" data-testid="link-add-from-dashboard"><Plus size={16} /> Add transaction</Link>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Total balance" value={money(summary.totalBalance, summary.currency)} detail={`${summary.balanceChange >= 0 ? '+' : '−'}${compactMoney(Math.abs(summary.balanceChange), summary.currency)} net this month`} icon={Wallet} /><StatCard label="Income this month" value={compactMoney(summary.monthlyIncome, summary.currency)} detail="Money in, so far" accent="gold" icon={ArrowDownLeft} /><StatCard label="Spent this month" value={compactMoney(summary.monthlyExpenses, summary.currency)} detail="Across all accounts" icon={ArrowUpRight} /><StatCard label="Budget to spare" value={compactMoney(summary.remainingBudget, summary.currency)} detail="Before month end" accent="gold" icon={Target} /></div><DashboardCharts summary={summary} /><div className="mt-5 grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><Card className="p-5 sm:p-6"><div className="mb-4 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Latest movement</p><h2 className="mt-2 font-serif text-2xl">Recent transactions</h2></div><Link href="/transactions" className="text-xs font-bold text-primary hover:underline" data-testid="link-see-transactions">See all</Link></div><div className="divide-y divide-border">{(summary.recentTransactions || []).slice(0, 5).map((tx) => <TransactionRow key={tx.id} transaction={tx} currency={summary.currency} />)}</div>{!summary.recentTransactions?.length && <EmptyState title="Your story starts here" body="Add your first transaction and Tereka will begin finding your rhythm." />}</Card><Insights insights={insights} loading={insightQuery.isLoading} /></div></AppShell>;
}

function DashboardSkeleton() { return <><div className="mb-8"><Skeleton className="h-3 w-28" /><Skeleton className="mt-4 h-12 w-80" /><Skeleton className="mt-3 h-4 w-96 max-w-full" /></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[1, 2, 3, 4].map((i) => <Skeleton key={i} className="h-32" />)}</div><div className="mt-5 grid gap-5 xl:grid-cols-2"><Skeleton className="h-80" /><Skeleton className="h-80" /></div></>; }

function Insights({ insights, loading }: { insights: any[]; loading: boolean }) {
  return <Card className="overflow-hidden"><div className="bg-primary p-5 text-primary-foreground sm:p-6"><div className="flex items-center gap-2 text-accent"><Sparkles size={17} /><span className="font-mono text-[10px] uppercase tracking-widest">Tereka noticed</span></div><h2 className="mt-3 font-serif text-2xl">A little more signal.</h2><p className="mt-2 text-xs leading-5 text-primary-foreground/65">Patterns worth seeing, without the noise.</p></div>{loading ? <div className="space-y-4 p-5"><Skeleton className="h-14" /><Skeleton className="h-14" /></div> : insights.length ? <div className="divide-y divide-border">{insights.slice(0, 3).map((insight) => <div key={insight.id} className="p-5"><p className="text-sm font-bold">{insight.title}</p><p className="mt-1.5 text-xs leading-5 text-muted-foreground">{insight.body}</p></div>)}</div> : <EmptyState title="The signal is forming" body="Keep logging your money and useful patterns will appear here." />}</Card>;
}

function TransactionRow({ transaction: tx, currency }: { transaction: Transaction; currency?: CurrencyType }) {
  return <div className="flex items-center gap-3 py-3"><span className={`grid h-9 w-9 place-items-center rounded-xl text-lg ${tx.type === 'income' ? 'bg-primary/10 text-primary' : 'bg-secondary text-primary'}`}>{transactionIcon(tx)}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{tx.description}</p><p className="truncate text-xs text-muted-foreground">{tx.categoryName} · {dateLabel(tx.transactionDate)}</p></div><p className={`font-mono text-xs font-medium ${tx.type === 'income' ? 'text-primary' : 'text-foreground'}`}>{tx.type === 'income' ? '+' : '−'}{money(tx.amount, tx.currency || currency)}</p></div>;
}

type TransactionForm = { type: TransactionTypeValue; amount: string; currency: CurrencyType; accountId: string; categoryId: string; description: string; notes: string; transactionDate: string };
const blankTx = (currency: CurrencyType = 'UGX'): TransactionForm => ({ type: 'expense', amount: '', currency, accountId: '', categoryId: '', description: '', notes: '', transactionDate: new Date().toISOString().slice(0, 10) });

function Transactions() {
  const qc = useQueryClient(); const [search, setSearch] = useState(''); const [type, setType] = useState<'all' | TransactionTypeValue>('all'); const [modal, setModal] = useState<'add' | Transaction | null>(null);
  const params = { search: search || undefined, type: type === 'all' ? undefined : type }; const query = useGetTransactions(params); const accounts = useGetAccounts(); const categories = useGetCategories(); const create = useCreateTransaction(); const update = useUpdateTransaction(); const remove = useDeleteTransaction();
  const list = query.data || []; const editing = modal && modal !== 'add' ? modal : null;
  const save = (form: TransactionForm) => { const data = { ...form, amount: Number(form.amount), notes: form.notes || undefined }; if (editing) update.mutate({ id: editing.id, data }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetTransactionsQueryKey(params) }); setModal(null); } }); else create.mutate({ data }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetTransactionsQueryKey(params) }); setModal(null); } }); };
  return <AppShell><PageHeading eyebrow="Money movement" title="Transactions" description="Every entry is a piece of context. Keep the picture honest." action={<Button onClick={() => setModal('add')} data-testid="button-add-transaction"><Plus size={16} /> Add transaction</Button>} /><Card className="overflow-hidden"><div className="flex flex-col gap-3 border-b border-border bg-secondary/35 p-4 sm:flex-row"><div className="relative flex-1"><Search size={16} className="absolute left-3 top-3.5 text-muted-foreground" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search descriptions or categories" className={`${inputClass} pl-10`} data-testid="input-search-transactions" /></div><div className="flex gap-2"><select value={type} onChange={(e) => setType(e.target.value as any)} className={`${inputClass} sm:w-36`} data-testid="select-transaction-type"><option value="all">All types</option><option value="income">Income</option><option value="expense">Expenses</option></select><Button variant="secondary" className="px-3" title="Filters are ready" data-testid="button-filter-transactions"><Filter size={16} /></Button></div></div>{query.isLoading ? <div className="space-y-4 p-5">{[1, 2, 3, 4, 5].map((i) => <Skeleton key={i} className="h-12" />)}</div> : query.isError ? <div className="p-5"><ErrorState retry={() => query.refetch()} /></div> : list.length ? <div className="divide-y divide-border">{list.map((tx) => <div key={tx.id} className="group flex items-center gap-3 px-4 py-1 hover:bg-secondary/25 sm:px-6"><div className="min-w-0 flex-1"><TransactionRow transaction={tx} /></div><div className="flex gap-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100"><button onClick={() => setModal(tx)} className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-primary" data-testid={`button-edit-transaction-${tx.id}`}><Edit3 size={15} /></button><button onClick={() => { if (window.confirm('Delete this transaction?')) remove.mutate({ id: tx.id }, { onSuccess: () => qc.invalidateQueries({ queryKey: getGetTransactionsQueryKey(params) }) }); }} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive" data-testid={`button-delete-transaction-${tx.id}`}><Trash2 size={15} /></button></div></div>)}</div> : <EmptyState title="No transactions here yet" body="Try another search, or add the first entry to your money story." action={<Button onClick={() => setModal('add')} data-testid="button-empty-add-transaction"><Plus size={15} /> Add transaction</Button>} />}</Card>{modal && <TransactionModal initial={editing || undefined} accounts={accounts.data || []} categories={categories.data || []} pending={create.isPending || update.isPending} onClose={() => setModal(null)} onSave={save} />}</AppShell>;
}

function TransactionModal({ initial, accounts, categories, pending, onClose, onSave }: { initial?: Transaction; accounts: Account[]; categories: Category[]; pending: boolean; onClose: () => void; onSave: (form: TransactionForm) => void }) {
  const [form, setForm] = useState<TransactionForm>(initial ? { type: initial.type, amount: String(initial.amount), currency: initial.currency, accountId: initial.accountId, categoryId: initial.categoryId, description: initial.description, notes: initial.notes || '', transactionDate: initial.transactionDate.slice(0, 10) } : blankTx(accounts[0]?.currency || 'UGX'));
  const set = (key: keyof TransactionForm, value: string) => setForm((prev) => ({ ...prev, [key]: value }));
  return <Modal title={initial ? 'Edit transaction' : 'Add transaction'} onClose={onClose}><form onSubmit={(e) => { e.preventDefault(); onSave(form); }} className="space-y-4"><div className="grid grid-cols-2 gap-3"><Field label="Type"><select value={form.type} onChange={(e) => set('type', e.target.value)} className={inputClass} data-testid="select-form-transaction-type"><option value="expense">Expense</option><option value="income">Income</option></select></Field><Field label="Amount"><input required type="number" min="1" value={form.amount} onChange={(e) => set('amount', e.target.value)} placeholder="0" className={inputClass} data-testid="input-form-transaction-amount" /></Field></div><Field label="Description"><input required value={form.description} onChange={(e) => set('description', e.target.value)} placeholder="What was this for?" className={inputClass} data-testid="input-form-transaction-description" /></Field><div className="grid grid-cols-2 gap-3"><Field label="Account"><select required value={form.accountId} onChange={(e) => { set('accountId', e.target.value); const account = accounts.find((a) => a.id === e.target.value); if (account) set('currency', account.currency); }} className={inputClass} data-testid="select-form-transaction-account"><option value="">Choose account</option>{accounts.filter((a) => a.isActive).map((a) => <option key={a.id} value={a.id}>{a.name}</option>)}</select></Field><Field label="Category"><select required value={form.categoryId} onChange={(e) => set('categoryId', e.target.value)} className={inputClass} data-testid="select-form-transaction-category"><option value="">Choose category</option>{categories.filter((c) => c.type === form.type).map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></Field></div><div className="grid grid-cols-2 gap-3"><Field label="Date"><input required type="date" value={form.transactionDate} onChange={(e) => set('transactionDate', e.target.value)} className={inputClass} data-testid="input-form-transaction-date" /></Field><Field label="Notes"><input value={form.notes} onChange={(e) => set('notes', e.target.value)} placeholder="Optional" className={inputClass} data-testid="input-form-transaction-notes" /></Field></div><Button type="submit" className="mt-2 w-full" disabled={pending} data-testid="button-save-transaction">{pending ? 'Saving…' : initial ? 'Save changes' : 'Add transaction'}</Button></form></Modal>;
}

function Accounts() {
  const qc = useQueryClient(); const query = useGetAccounts(); const [modal, setModal] = useState<'add' | Account | null>(null); const create = useCreateAccount(); const update = useUpdateAccount(); const archive = useArchiveAccount(); const accounts = query.data || [];
  const save = (data: any, id?: string) => { const onSuccess = () => { qc.invalidateQueries({ queryKey: getGetAccountsQueryKey() }); setModal(null); }; if (id) update.mutate({ id, data }, { onSuccess }); else create.mutate({ data }, { onSuccess }); };
  return <AppShell><PageHeading eyebrow="Your foundations" title="Accounts" description="The places your money rests, moves through, and grows." action={<Button onClick={() => setModal('add')} data-testid="button-add-account"><Plus size={16} /> Add account</Button>} /><div className="mb-6 grid gap-4 sm:grid-cols-3"><Card className="bg-primary p-5 text-primary-foreground sm:col-span-2"><p className="font-mono text-[10px] uppercase tracking-widest text-primary-foreground/60">Across active accounts</p><p className="mt-3 font-serif text-4xl">{money(accounts.filter((a) => a.isActive).reduce((sum, a) => sum + a.balance, 0), accounts[0]?.currency)}</p><p className="mt-2 text-xs text-primary-foreground/60">Your connected view, in one place.</p></Card><Card className="flex flex-col justify-between p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Active accounts</p><p className="mt-4 text-4xl font-extrabold">{accounts.filter((a) => a.isActive).length}</p><p className="text-xs text-muted-foreground">Keep it simple and useful.</p></Card></div>{query.isLoading ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[1, 2, 3].map((i) => <Skeleton key={i} className="h-48" />)}</div> : query.isError ? <ErrorState retry={() => query.refetch()} /> : accounts.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{accounts.map((account) => <Card key={account.id} className={`group p-5 ${!account.isActive ? 'opacity-60' : ''}`}><div className="flex items-start justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-secondary text-primary"><Landmark size={19} /></span><div className="flex gap-1"><button onClick={() => setModal(account)} className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-primary" data-testid={`button-edit-account-${account.id}`}><Edit3 size={15} /></button>{account.isActive && <button onClick={() => { if (window.confirm('Archive this account? Its history will be kept.')) archive.mutate({ id: account.id }, { onSuccess: () => qc.invalidateQueries({ queryKey: getGetAccountsQueryKey() }) }); }} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive" data-testid={`button-archive-account-${account.id}`}><MoreHorizontal size={15} /></button>}</div></div><p className="mt-5 text-sm font-bold">{account.name}</p><p className="mt-1 text-xs capitalize text-muted-foreground">{account.type.replace('_', ' ')} · {account.currency}</p><p className="mt-5 font-mono text-2xl">{money(account.balance, account.currency)}</p><p className="mt-1 text-xs text-muted-foreground">{account.isActive ? 'Active balance' : 'Archived account'}</p></Card>)}</div> : <Card><EmptyState title="Give your money a home" body="Add a bank, mobile wallet, or cash account to see your full balance." action={<Button onClick={() => setModal('add')} data-testid="button-empty-add-account"><Plus size={15} /> Add account</Button>} /></Card>}{modal && <AccountModal initial={modal !== 'add' ? modal : undefined} pending={create.isPending || update.isPending} onClose={() => setModal(null)} onSave={save} />}</AppShell>;
}

function AccountModal({ initial, pending, onClose, onSave }: { initial?: Account; pending: boolean; onClose: () => void; onSave: (data: any, id?: string) => void }) {
  const [name, setName] = useState(initial?.name || ''); const [type, setType] = useState(initial?.type || 'bank'); const [currency, setCurrency] = useState<CurrencyType>(initial?.currency || 'UGX'); const [opening, setOpening] = useState(String(initial?.openingBalance || ''));
  return <Modal title={initial ? 'Edit account' : 'Add account'} onClose={onClose}><form onSubmit={(e) => { e.preventDefault(); onSave({ name, type, currency, openingBalance: Number(opening) }, initial?.id); }} className="space-y-4"><Field label="Account name"><input required value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. M-Pesa everyday" className={inputClass} data-testid="input-account-name" /></Field><div className="grid grid-cols-2 gap-3"><Field label="Type"><select value={type} onChange={(e) => setType(e.target.value as typeof type)} className={inputClass} data-testid="select-account-type"><option value="bank">Bank</option><option value="mobile_money">Mobile money</option><option value="cash">Cash</option><option value="savings">Savings</option><option value="other">Other</option></select></Field><Field label="Currency"><select value={currency} onChange={(e) => setCurrency(e.target.value as CurrencyType)} className={inputClass} data-testid="select-account-currency">{currencies.map((c) => <option key={c} value={c}>{c}</option>)}</select></Field></div><Field label="Opening balance"><input required type="number" min="0" value={opening} onChange={(e) => setOpening(e.target.value)} className={inputClass} data-testid="input-account-opening" /></Field><Button className="w-full" type="submit" disabled={pending} data-testid="button-save-account">{pending ? 'Saving…' : initial ? 'Save changes' : 'Add account'}</Button></form></Modal>;
}

function Budgets() {
  const qc = useQueryClient(); const query = useGetBudgets(); const categories = useGetCategories({ type: 'expense' }); const createCategory = useCreateCategory(); const [modal, setModal] = useState<'add' | Budget | null>(null); const [categoryModal, setCategoryModal] = useState(false); const create = useCreateBudget(); const update = useUpdateBudget(); const remove = useDeleteBudget(); const budgets = query.data || [];
  const save = (data: any, id?: string) => { const onSuccess = () => { qc.invalidateQueries({ queryKey: getGetBudgetsQueryKey() }); setModal(null); }; if (id) update.mutate({ id, data }, { onSuccess }); else create.mutate({ data }, { onSuccess }); };
  return <AppShell><PageHeading eyebrow="Spend with intention" title="Budgets" description="A gentle guardrail for the things that matter this month." action={<Button onClick={() => setModal('add')} data-testid="button-add-budget"><Plus size={16} /> New budget</Button>} />{query.isLoading ? <div className="grid gap-4 sm:grid-cols-2">{[1, 2, 3].map((i) => <Skeleton key={i} className="h-48" />)}</div> : query.isError ? <ErrorState retry={() => query.refetch()} /> : budgets.length ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{budgets.map((budget, i) => <Card key={budget.id} className="p-5"><div className="flex items-start justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-secondary text-primary">{['⌁', '⌂', '→', '•'][i % 4]}</span><div><p className="text-sm font-bold">{budget.categoryName}</p><p className="mt-0.5 text-xs capitalize text-muted-foreground">{budget.period} plan</p></div></div><div className="flex gap-1"><button onClick={() => setModal(budget)} className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-primary" data-testid={`button-edit-budget-${budget.id}`}><Edit3 size={15} /></button><button onClick={() => { if (window.confirm('Delete this budget?')) remove.mutate({ id: budget.id }, { onSuccess: () => qc.invalidateQueries({ queryKey: getGetBudgetsQueryKey() }) }); }} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive" data-testid={`button-delete-budget-${budget.id}`}><Trash2 size={15} /></button></div></div><div className="mt-7 flex items-end justify-between"><p className="font-mono text-xl">{money(budget.spent, budget.currency)} <span className="text-xs text-muted-foreground">of {money(budget.amount, budget.currency)}</span></p><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${budget.status === 'exceeded' ? 'bg-destructive/10 text-destructive' : budget.status === 'warning' ? 'bg-accent/25 text-foreground' : 'bg-primary/10 text-primary'}`}>{budget.status.replace('_', ' ')}</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-secondary"><div className={`h-full rounded-full ${budget.status === 'exceeded' ? 'bg-destructive' : 'bg-primary'}`} style={{ width: `${Math.min(100, budget.percentageUsed)}%` }} /></div><p className="mt-2 text-right font-mono text-[10px] text-muted-foreground">{budget.percentageUsed}% used</p></Card>)}</div> : <Card><EmptyState title="A plan for your month" body="Set a budget for one category. It can be a guide, not a restriction." action={<Button onClick={() => setModal('add')} data-testid="button-empty-add-budget"><Plus size={15} /> Create a budget</Button>} /></Card>}<div className="mt-8 flex items-center justify-between border-t border-border pt-5"><div><p className="text-sm font-bold">Need another category?</p><p className="mt-1 text-xs text-muted-foreground">Create a custom label for your real life.</p></div><Button variant="secondary" onClick={() => setCategoryModal(true)} data-testid="button-add-category"><Plus size={15} /> New category</Button></div>{modal && <BudgetModal initial={modal !== 'add' ? modal : undefined} categories={categories.data || []} pending={create.isPending || update.isPending} onClose={() => setModal(null)} onSave={save} />}{categoryModal && <CategoryModal pending={createCategory.isPending} onClose={() => setCategoryModal(false)} onSave={(data) => createCategory.mutate({ data }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetCategoriesQueryKey({ type: 'expense' }) }); setCategoryModal(false); } })} />}</AppShell>;
}

function BudgetModal({ initial, categories, pending, onClose, onSave }: { initial?: Budget; categories: Category[]; pending: boolean; onClose: () => void; onSave: (data: any, id?: string) => void }) {
  const [categoryId, setCategoryId] = useState(initial?.categoryId || categories[0]?.id || ''); const [amount, setAmount] = useState(String(initial?.amount || '')); const [currency, setCurrency] = useState<CurrencyType>(initial?.currency || 'UGX');
  return <Modal title={initial ? 'Edit budget' : 'New monthly budget'} onClose={onClose}><form onSubmit={(e) => { e.preventDefault(); onSave({ categoryId, amount: Number(amount), currency, period: 'monthly' }, initial?.id); }} className="space-y-4"><Field label="Category"><select required value={categoryId} onChange={(e) => setCategoryId(e.target.value)} className={inputClass} data-testid="select-budget-category">{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><div className="grid grid-cols-2 gap-3"><Field label="Monthly amount"><input required type="number" min="1" value={amount} onChange={(e) => setAmount(e.target.value)} className={inputClass} data-testid="input-budget-amount" /></Field><Field label="Currency"><select value={currency} onChange={(e) => setCurrency(e.target.value as CurrencyType)} className={inputClass} data-testid="select-budget-currency">{currencies.map((c) => <option key={c} value={c}>{c}</option>)}</select></Field></div><Button className="w-full" type="submit" disabled={pending} data-testid="button-save-budget">{pending ? 'Saving…' : initial ? 'Save changes' : 'Create budget'}</Button></form></Modal>;
}

function CategoryModal({ pending, onClose, onSave }: { pending: boolean; onClose: () => void; onSave: (data: any) => void }) { const [name, setName] = useState(''); return <Modal title="New category" onClose={onClose}><form className="space-y-4" onSubmit={(e) => { e.preventDefault(); onSave({ name, type: 'expense', icon: 'circle' }); }}><Field label="Category name"><input required value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Family support" className={inputClass} data-testid="input-category-name" /></Field><Button className="w-full" type="submit" disabled={pending} data-testid="button-save-category">{pending ? 'Creating…' : 'Create category'}</Button></form></Modal>; }

function Goals() {
  const qc = useQueryClient(); const query = useGetGoals(); const [modal, setModal] = useState<'add' | FinancialGoal | null>(null); const create = useCreateGoal(); const update = useUpdateGoal(); const remove = useDeleteGoal(); const goals = query.data || [];
  const save = (data: any, id?: string) => { const onSuccess = () => { qc.invalidateQueries({ queryKey: getGetGoalsQueryKey() }); setModal(null); }; if (id) update.mutate({ id, data }, { onSuccess }); else create.mutate({ data }, { onSuccess }); };
  return <AppShell><PageHeading eyebrow="Forward motion" title="Goals" description="The things your money is making possible — one contribution at a time." action={<Button onClick={() => setModal('add')} data-testid="button-add-goal"><Plus size={16} /> New goal</Button>} />{query.isLoading ? <div className="grid gap-4 sm:grid-cols-2">{[1, 2].map((i) => <Skeleton key={i} className="h-52" />)}</div> : query.isError ? <ErrorState retry={() => query.refetch()} /> : goals.length ? <div className="grid gap-4 lg:grid-cols-2">{goals.map((goal) => <Card key={goal.id} className="p-5 sm:p-6"><div className="flex items-start justify-between"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-accent/25 text-primary"><Target size={20} /></span><div><h2 className="font-serif text-xl">{goal.name}</h2><p className="mt-1 text-xs text-muted-foreground">Target {dateLabel(goal.targetDate, true)}</p></div></div><div className="flex gap-1"><button onClick={() => setModal(goal)} className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-primary" data-testid={`button-edit-goal-${goal.id}`}><Edit3 size={15} /></button><button onClick={() => { if (window.confirm('Delete this goal?')) remove.mutate({ id: goal.id }, { onSuccess: () => qc.invalidateQueries({ queryKey: getGetGoalsQueryKey() }) }); }} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive" data-testid={`button-delete-goal-${goal.id}`}><Trash2 size={15} /></button></div></div><div className="mt-7 flex items-end justify-between"><div><p className="font-mono text-2xl">{money(goal.currentAmount, goal.currency)}</p><p className="mt-1 text-xs text-muted-foreground">of {money(goal.targetAmount, goal.currency)}</p></div><p className="font-mono text-xl text-primary">{goal.percentageComplete}%</p></div><div className="mt-4 h-3 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary transition-all" style={{ width: `${Math.min(100, goal.percentageComplete)}%` }} /></div><div className="mt-3 flex justify-between text-xs text-muted-foreground"><span>{goal.status}</span><span>{money(goal.remainingAmount, goal.currency)} to go</span></div></Card>)}</div> : <Card><EmptyState title="Give a goal a number" body="A named destination makes progress easier to feel." action={<Button onClick={() => setModal('add')} data-testid="button-empty-add-goal"><Plus size={15} /> Set a goal</Button>} /></Card>}{modal && <GoalModal initial={modal !== 'add' ? modal : undefined} pending={create.isPending || update.isPending} onClose={() => setModal(null)} onSave={save} />}</AppShell>;
}

function GoalModal({ initial, pending, onClose, onSave }: { initial?: FinancialGoal; pending: boolean; onClose: () => void; onSave: (data: any, id?: string) => void }) { const [name, setName] = useState(initial?.name || ''); const [target, setTarget] = useState(String(initial?.targetAmount || '')); const [current, setCurrent] = useState(String(initial?.currentAmount || '')); const [currency, setCurrency] = useState<CurrencyType>(initial?.currency || 'UGX'); const [date, setDate] = useState(initial?.targetDate?.slice(0, 10) || ''); const [status, setStatus] = useState(initial?.status || 'active'); return <Modal title={initial ? 'Edit goal' : 'New savings goal'} onClose={onClose}><form className="space-y-4" onSubmit={(e) => { e.preventDefault(); onSave({ name, targetAmount: Number(target), currentAmount: Number(current), currency, targetDate: date, status }, initial?.id); }}><Field label="What are you building toward?"><input required value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. A home down payment" className={inputClass} data-testid="input-goal-name" /></Field><div className="grid grid-cols-2 gap-3"><Field label="Target amount"><input required type="number" min="1" value={target} onChange={(e) => setTarget(e.target.value)} className={inputClass} data-testid="input-goal-target" /></Field><Field label="Already saved"><input required type="number" min="0" value={current} onChange={(e) => setCurrent(e.target.value)} className={inputClass} data-testid="input-goal-current" /></Field></div><div className="grid grid-cols-2 gap-3"><Field label="Target date"><input required type="date" value={date} onChange={(e) => setDate(e.target.value)} className={inputClass} data-testid="input-goal-date" /></Field><Field label="Currency"><select value={currency} onChange={(e) => setCurrency(e.target.value as CurrencyType)} className={inputClass} data-testid="select-goal-currency">{currencies.map((c) => <option key={c} value={c}>{c}</option>)}</select></Field></div><Field label="Status"><select value={status} onChange={(e) => setStatus(e.target.value as any)} className={inputClass} data-testid="select-goal-status"><option value="active">Active</option><option value="paused">Paused</option><option value="completed">Completed</option></select></Field><Button className="w-full" type="submit" disabled={pending} data-testid="button-save-goal">{pending ? 'Saving…' : initial ? 'Save changes' : 'Create goal'}</Button></form></Modal>; }

export function Assistant() {
  const qc = useQueryClient(); const conversationsQuery = useGetConversations(); const [conversationId, setConversationId] = useState(''); const [draft, setDraft] = useState('');
  const conversations = conversationsQuery.data || []; const activeId = conversationId || conversations[0]?.id || ''; const messagesQuery = useGetConversationMessages(activeId, { query: { enabled: Boolean(activeId), queryKey: getGetConversationMessagesQueryKey(activeId) } }); const createConversation = useCreateConversation(); const send = useSendAssistantMessage();
  const newConversation = () => createConversation.mutate({ data: { title: 'New money conversation' } }, { onSuccess: (conversation) => { setConversationId(conversation.id); qc.invalidateQueries({ queryKey: getGetConversationsQueryKey() }); } });
  const submit = () => { if (!draft.trim() || !activeId) return; const content = draft.trim(); setDraft(''); send.mutate({ id: activeId, data: { content } }, { onSuccess: (messages) => qc.setQueryData(getGetConversationMessagesQueryKey(activeId), messages) }); };
  return <AppShell><PageHeading eyebrow="Your thinking partner" title="Tereka AI" description="Ask the questions that are hard to answer from a spreadsheet. Tereka keeps it practical." action={<Button variant="secondary" onClick={newConversation} disabled={createConversation.isPending} data-testid="button-new-conversation"><Plus size={16} /> New conversation</Button>} /><div className="grid min-h-[560px] gap-5 lg:grid-cols-[250px_1fr]"><Card className="hidden p-3 lg:block"><p className="px-3 py-3 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Your conversations</p><div className="space-y-1">{conversations.map((conversation: Conversation) => <button key={conversation.id} onClick={() => setConversationId(conversation.id)} className={`w-full rounded-xl px-3 py-3 text-left text-sm ${activeId === conversation.id ? 'bg-secondary font-bold text-primary' : 'text-muted-foreground hover:bg-secondary/60'}`} data-testid={`button-conversation-${conversation.id}`}><span className="block truncate">{conversation.title}</span><span className="mt-1 block font-mono text-[10px] font-normal opacity-60">{dateLabel(conversation.updatedAt)}</span></button>)}</div>{!conversations.length && <p className="px-3 py-4 text-xs leading-5 text-muted-foreground">Start a conversation and your questions will live here.</p>}</Card><Card className="flex min-h-[560px] flex-col overflow-hidden"><div className="flex items-center gap-3 border-b border-border bg-secondary/30 p-4 sm:p-5"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-primary text-primary-foreground"><Sparkles size={18} /></span><div><p className="text-sm font-bold">{conversations.find((c) => c.id === activeId)?.title || 'A fresh start'}</p><p className="mt-0.5 text-xs text-muted-foreground">Personal, practical, private</p></div></div>{!activeId ? <EmptyState title="Start with a question" body="Try asking what changed in your spending this month, or how to make room for a goal." action={<Button onClick={newConversation} data-testid="button-start-conversation"><Sparkles size={15} /> Start conversation</Button>} /> : <><div className="flex-1 space-y-4 overflow-y-auto p-5 sm:p-7">{messagesQuery.isLoading ? <><Skeleton className="h-16 w-3/4" /><Skeleton className="ml-auto h-12 w-2/3" /></> : messagesQuery.isError ? <ErrorState retry={() => messagesQuery.refetch()} /> : (messagesQuery.data || []).length ? (messagesQuery.data || []).map((message) => <div key={message.id} className={`flex ${message.role === 'user' ? 'justify-end' : 'justify-start'}`}><div className={`max-w-[88%] rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === 'user' ? 'rounded-br-sm bg-primary text-primary-foreground' : 'rounded-bl-sm bg-secondary text-foreground'}`} data-testid={`message-${message.id}`}>{message.content}</div></div>) : <div className="py-10 text-center"><p className="font-serif text-2xl">What would make money feel clearer today?</p><div className="mt-5 flex flex-wrap justify-center gap-2">{['Where did I spend most this month?', 'Can I afford my savings goal?', 'Help me make a simple plan'].map((q) => <button key={q} onClick={() => setDraft(q)} className="rounded-full border border-border px-3 py-2 text-xs font-semibold text-muted-foreground hover:border-primary hover:text-primary" data-testid={`button-suggestion-${q.slice(0, 5)}`}>{q}</button>)}</div></div>}{send.isPending && <div className="flex"><div className="rounded-2xl rounded-bl-sm bg-secondary px-4 py-3 text-xs text-muted-foreground">Thinking through it…</div></div>}</div><div className="border-t border-border p-4"><div className="flex items-end gap-2 rounded-2xl border border-input bg-background p-2 focus-within:border-primary"><textarea value={draft} onChange={(e) => setDraft(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); submit(); } }} placeholder="Ask about your money…" rows={1} className="max-h-28 min-h-10 flex-1 resize-none bg-transparent px-2 py-2 text-sm outline-none placeholder:text-muted-foreground" data-testid="input-assistant-message" /><button onClick={submit} disabled={send.isPending || !draft.trim()} className="grid h-10 w-10 place-items-center rounded-xl bg-primary text-primary-foreground disabled:opacity-40" data-testid="button-send-assistant"><Send size={16} /></button></div><p className="mt-2 text-center font-mono text-[9px] uppercase tracking-widest text-muted-foreground/60">Enter to send · Shift + Enter for a new line</p></div></>}</Card></div></AppShell>;
}

export function Settings() {
  const qc = useQueryClient(); const query = useGetProfile(); const update = useUpdateProfile(); const profile = query.data; const [name, setName] = useState(''); const [country, setCountry] = useState(''); const [currency, setCurrency] = useState<CurrencyType>('UGX'); const [theme, setTheme] = useState<ProfileTheme>('light'); const [initialized, setInitialized] = useState(false);
  if (profile && !initialized) { setName(profile.fullName); setCountry(profile.country); setCurrency(profile.preferredCurrency); setTheme(profile.theme); setInitialized(true); }
  useEffect(() => { if (initialized) applyTheme(theme); }, [theme, initialized]);
  const save = () => update.mutate({ data: { fullName: name, country, preferredCurrency: currency, theme } }, { onSuccess: (data) => { qc.setQueryData(getGetProfileQueryKey(), data); applyTheme(theme); } });
  if (query.isLoading) return <AppShell><Skeleton className="h-12 w-72" /><Skeleton className="mt-8 h-80 max-w-2xl" /></AppShell>;
  if (query.isError || !profile) return <AppShell><ErrorState retry={() => query.refetch()} /></AppShell>;
  return <AppShell><PageHeading eyebrow="Your preferences" title="Settings" description="Make Tereka feel like your own space." /><div className="max-w-3xl space-y-5"><Card className="p-5 sm:p-7"><div className="mb-6 flex items-center gap-4"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-primary font-serif text-xl text-primary-foreground">{name.slice(0, 1).toUpperCase()}</span><div><h2 className="font-serif text-2xl">{name || 'Your profile'}</h2><p className="mt-1 text-sm text-muted-foreground">{profile.email}</p></div></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name"><input value={name} onChange={(e) => setName(e.target.value)} className={inputClass} data-testid="input-profile-name" /></Field><Field label="Country"><input value={country} onChange={(e) => setCountry(e.target.value)} placeholder="Uganda" className={inputClass} data-testid="input-profile-country" /></Field><Field label="Preferred currency"><select value={currency} onChange={(e) => setCurrency(e.target.value as CurrencyType)} className={inputClass} data-testid="select-profile-currency">{currencies.map((c) => <option key={c} value={c}>{c}</option>)}</select></Field></div></Card><Card className="p-5 sm:p-7"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Appearance</p><h2 className="mt-2 font-serif text-2xl">Choose your atmosphere</h2><div className="mt-5 grid gap-3 sm:grid-cols-3">{(['light', 'dark', 'system'] as ProfileTheme[]).map((option) => <button key={option} onClick={() => setTheme(option)} className={`rounded-2xl border p-4 text-left ${theme === option ? 'border-primary bg-secondary' : 'border-border hover:border-primary/40'}`} data-testid={`button-theme-${option}`}><span className={`mb-4 block h-12 rounded-xl ${option === 'dark' ? 'bg-sidebar' : option === 'system' ? 'bg-gradient-to-r from-background via-background to-sidebar' : 'bg-background border border-border'}`} /><p className="text-sm font-bold capitalize">{option}</p><p className="mt-1 text-xs text-muted-foreground">{option === 'system' ? 'Follow device' : `Use ${option} mode`}</p></button>)}</div></Card><div className="flex items-center justify-between"><p className="text-xs text-muted-foreground">{update.isSuccess ? 'Saved just now.' : 'Changes are saved when you choose Save.'}</p><Button onClick={save} disabled={update.isPending} data-testid="button-save-settings">{update.isPending ? 'Saving…' : 'Save preferences'} <Check size={16} /></Button></div></div></AppShell>;
}

export { Dashboard, Transactions, Accounts, Budgets, Goals };