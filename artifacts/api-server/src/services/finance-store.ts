/**
 * @deprecated RETIRED: All finance and authentication operations have been rewired directly to PostgreSQL via Drizzle ORM in `@workspace/db`.
 * No in-memory arrays or phantom stores are served.
 */
export {};